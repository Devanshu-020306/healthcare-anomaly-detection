services:
  - type: web
    name: healthcare-backend
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r backend/requirements_simple.txt
    startCommand: python backend/app_simple.py
    envVars:
      - key: PORT
        value: 5000
      - key: PYTHON_VERSION
        value: 3.11.0
